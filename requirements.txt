# Runtime dependencies for the dashboard and the study.
# Streamlit Community Cloud installs from this file.
#
# Development extras (pytest, ruff) are NOT listed here on purpose: they would
# slow every cloud rebuild for no benefit. Install them with:
#     pip install -e ".[dev]"

numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
plotly>=5.15
streamlit>=1.30

# Tabulated CIE 1931 colour-matching functions and D65.
# Optional: finishsim falls back to the Wyman et al. (2013) analytic fit if it
# is absent, and reports which path was taken via colourimetry.CMF_SOURCE.
colour-science>=0.4.3
